/// <reference types="node" />
import { Cluster } from "cluster";
import NodeCache from "node-cache";
export declare class TenantClusterCache {
    private namespace;
    private ttl;
    private cache?;
    private resolvers;
    constructor(cluster: Cluster, namespace: string, ttl: number);
    private prefixData;
    get<T>(tenantName: string, key: NodeCache.Key, storeFn?: () => Promise<T>): Promise<T | undefined>;
    del(tenantName: string, keys: string | number | NodeCache.Key): Promise<unknown>;
    set<T>(tenantName: string, key: NodeCache.Key, val: T, ttl?: number): Promise<boolean>;
    flush(tenantName: string): void;
    getStats(): Promise<Map<string, NodeCache.Stats>>;
}
